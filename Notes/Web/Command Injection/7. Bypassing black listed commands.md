
Just a example (AIM was to find the file in home directory)
![[Pasted image 20250929204414.png]]

---

Using PHP command can be simply blacklisted like this
```php
$blacklist = ['whoami', 'cat', ...SNIP...];
foreach ($blacklist as $word) {
    if (strpos('$_POST['ip']', $word) !== false) {
        echo "Invalid input";
    }
}
```

---

## Linux & Windows

```shell-session
w'h'o'am'i
```

```shell-session
w"h"o"am"i
```

NOTE: `we cannot mix types of quotes` and `the number of quotes must be even`\

---

## Linux Only

```bash
who$@ami
```

```bash
w\ho\am\i
```


---

## Windows Only

```cmd-session
who^ami
```

