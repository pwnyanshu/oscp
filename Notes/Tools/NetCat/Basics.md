nc -lvnp 7777

- **l** → _listen mode_ (waits for incoming connections instead of initiating one)
- **v** → _verbose_ (shows detailed output — what’s happening)
- **n** → _numeric only_ (doesn’t try to resolve hostnames via DNS)
- **p** → _port_ (specifies the port number to listen on)